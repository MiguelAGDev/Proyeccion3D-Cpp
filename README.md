# Proyeccion3D-Cpp
Visualizador de gráficos en C++ usando matrices (x,y,z,flag). Implementa transformaciones geométricas (rotación, traslación) y mapeo Window-to-Viewport. / Graphics viewer in C++ using matrices (x,y,z,flag), geometric transforms (rotation, translation) and Window-to-Viewport mapping.
